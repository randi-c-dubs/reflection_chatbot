// Config starter code
import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [createChatBotMessage(`Welcome to the Reflection Chatbot. What would you like to chat about?`)],
};

export default config;
